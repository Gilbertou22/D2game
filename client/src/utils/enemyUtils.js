// 敵人工具函數 (目前空殼，之後可擴展)
export const checkEnemyDeath = (enemy, index) => {
    // 死亡掉落、經驗邏輯移到這裡
    console.log('Enemy died', enemy, index);
};

export const createEnemyProjectile = () => {
    // 遠程怪物投射物
};